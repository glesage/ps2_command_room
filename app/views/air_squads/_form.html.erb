<%= form_for(@air_squad) do |f| %>
  <% if @air_squad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@air_squad.errors.count, "error") %> prohibited this air_squad from being saved:</h2>

      <ul>
      <% @air_squad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :platoon %><br>
    <%= f.number_field :platoon %>
  </div>
  <div class="field">
    <%= f.label :squad %><br>
    <%= f.number_field :squad %>
  </div>
  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.select :room_id, @room_choice %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
