<%= form_for(@control) do |f| %>
  <% if @control.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@control.errors.count, "error") %> prohibited this control from being saved:</h2>

      <ul>
      <% @control.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NC %><br>
    <%= f.text_field :NC %>
  </div>
  <div class="field">
    <%= f.label :TR %><br>
    <%= f.text_field :TR %>
  </div>
  <div class="field">
    <%= f.label :VS %><br>
    <%= f.text_field :VS %>
  </div>
  <div class="field">
    <%= f.label :map_id %><br>
    <%= f.select :map_id, @map_choice %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
