<%= form_for(@map) do |f| %>
  <% if @map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
      <% @map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ps2_id %><br>
    <%= f.number_field :ps2_id %>
  </div>
  <div class="field">
    <%= f.label :server_id %><br>
    <%= f.select :server_id, @server_choice %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
